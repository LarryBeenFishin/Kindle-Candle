<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Candle Fragrance Load Calculator | Kindle Candle</title>
  <meta name="description" content="A free candle fragrance load calculator styled for Kindle Candle. Calculate wax and fragrance oil for any batch size." />
  <style>
    :root {
      --ink: #211a14;
      --brown: #4a3424;
      --brown-2: #6a4b32;
      --paper: #f4ede2;
      --paper-2: #eadcc9;
      --kraft: #cfb896;
      --line: rgba(33, 26, 20, 0.30);
      --muted: #74685c;
      --white: #fffdf8;
      --shadow: 0 18px 50px rgba(60, 42, 28, .13);
      --radius: 2px;
    }

    * { box-sizing: border-box; }

    html { scroll-behavior: smooth; }

    body {
      margin: 0;
      color: var(--ink);
      background:
        radial-gradient(circle at 12% 18%, rgba(83,55,34,.035) 0 1px, transparent 1.5px),
        radial-gradient(circle at 78% 68%, rgba(83,55,34,.028) 0 1px, transparent 1.5px),
        var(--paper);
      background-size: 18px 18px, 23px 23px, auto;
      font-family: "Courier New", Courier, monospace;
      min-height: 100vh;
    }

    a { color: inherit; }

    .announcement {
      background: var(--ink);
      color: var(--paper);
      text-align: center;
      padding: 9px 16px 8px;
      font-size: 11px;
      letter-spacing: .16em;
      text-transform: uppercase;
    }

    .site-header {
      max-width: 1180px;
      margin: 0 auto;
      padding: 22px 28px 18px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 24px;
      border-bottom: 1px solid var(--line);
    }

    .brand {
      display: inline-flex;
      align-items: center;
      gap: 12px;
      text-decoration: none;
      text-transform: uppercase;
      letter-spacing: .16em;
      font-size: 15px;
      font-weight: 700;
    }

    .brand-mark {
      width: 27px;
      height: 27px;
      border: 1.5px solid var(--ink);
      border-radius: 50% 50% 48% 48%;
      display: grid;
      place-items: center;
      position: relative;
    }

    .brand-mark::before {
      content: "";
      width: 7px;
      height: 12px;
      border-radius: 55% 45% 55% 45%;
      border: 1.5px solid var(--ink);
      transform: rotate(12deg);
    }

    .brand-mark::after {
      content: "";
      position: absolute;
      bottom: -5px;
      width: 1px;
      height: 7px;
      background: var(--ink);
    }

    .nav {
      display: flex;
      gap: 26px;
      font-size: 11px;
      letter-spacing: .12em;
      text-transform: uppercase;
    }

    .nav a {
      text-decoration: none;
      border-bottom: 1px solid transparent;
      padding-bottom: 3px;
    }

    .nav a:hover { border-color: var(--ink); }

    main {
      max-width: 1180px;
      margin: 0 auto;
      padding: 78px 28px 96px;
    }

    .hero {
      max-width: 860px;
      margin: 0 auto 42px;
      text-align: center;
    }

    .eyebrow {
      display: inline-block;
      margin-bottom: 18px;
      padding-bottom: 4px;
      border-bottom: 1px solid var(--ink);
      font-size: 11px;
      letter-spacing: .18em;
      text-transform: uppercase;
    }

    h1 {
      margin: 0;
      font-size: clamp(35px, 5vw, 66px);
      line-height: .98;
      letter-spacing: -.045em;
      font-weight: 500;
    }

    .hero p {
      max-width: 710px;
      margin: 22px auto 0;
      color: var(--muted);
      font-family: Georgia, "Times New Roman", serif;
      font-size: 17px;
      line-height: 1.65;
    }

    .tool-shell {
      max-width: 950px;
      margin: 0 auto;
      background: rgba(255, 253, 248, .68);
      border: 1px solid var(--ink);
      box-shadow: var(--shadow);
      position: relative;
    }

    .tool-shell::before {
      content: "KINDLE CANDLE • MAKER TOOL";
      position: absolute;
      top: -12px;
      left: 50%;
      transform: translateX(-50%);
      background: var(--paper);
      padding: 3px 12px;
      white-space: nowrap;
      font-size: 10px;
      letter-spacing: .18em;
    }

    .calculator {
      display: grid;
      grid-template-columns: minmax(0, 1.05fr) minmax(300px, .95fr);
    }

    .inputs,
    .results {
      padding: 42px;
    }

    .results {
      background: var(--paper-2);
      border-left: 1px solid var(--ink);
      display: flex;
      flex-direction: column;
      justify-content: space-between;
    }

    .section-title {
      margin: 0 0 28px;
      font-size: 15px;
      letter-spacing: .16em;
      text-transform: uppercase;
      font-weight: 700;
    }

    .field-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 22px 18px;
    }

    .field.full { grid-column: 1 / -1; }

    label {
      display: block;
      margin-bottom: 8px;
      font-size: 12px;
      letter-spacing: .06em;
      font-weight: 700;
    }

    .help {
      display: block;
      color: var(--muted);
      font-family: Georgia, "Times New Roman", serif;
      font-size: 12px;
      line-height: 1.45;
      margin-top: 7px;
    }

    input,
    select {
      width: 100%;
      border: 1px solid var(--ink);
      border-radius: 0;
      background: var(--white);
      color: var(--ink);
      padding: 14px 13px;
      min-height: 48px;
      font: inherit;
      font-size: 16px;
      outline: none;
      transition: box-shadow .15s ease, background .15s ease;
    }

    input:focus,
    select:focus {
      box-shadow: 0 0 0 3px rgba(74, 52, 36, .13);
      background: #fff;
    }

    .input-addon {
      display: grid;
      grid-template-columns: 1fr 92px;
    }

    .input-addon input { border-right: 0; }

    .percent-wrap {
      position: relative;
    }

    .percent-wrap input { padding-right: 44px; }

    .percent-sign {
      position: absolute;
      right: 15px;
      top: 50%;
      transform: translateY(-50%);
      font-size: 15px;
      color: var(--muted);
      pointer-events: none;
    }

    .button-row {
      display: flex;
      gap: 12px;
      margin-top: 30px;
    }

    button {
      appearance: none;
      border: 1px solid var(--ink);
      border-radius: 0;
      cursor: pointer;
      font: inherit;
      letter-spacing: .12em;
      text-transform: uppercase;
      transition: transform .12s ease, box-shadow .12s ease, background .12s ease;
    }

    .calculate {
      flex: 1;
      background: var(--ink);
      color: var(--paper);
      padding: 15px 18px;
      font-size: 12px;
      font-weight: 700;
    }

    .calculate:hover {
      transform: translateY(-1px);
      box-shadow: 4px 4px 0 var(--kraft);
    }

    .reset {
      background: transparent;
      color: var(--ink);
      padding: 15px 18px;
      font-size: 11px;
    }

    .result-stack {
      border-top: 1px solid var(--ink);
    }

    .result-row {
      display: grid;
      grid-template-columns: 1fr auto;
      gap: 20px;
      align-items: end;
      padding: 20px 0 18px;
      border-bottom: 1px solid var(--line);
    }

    .result-label {
      font-size: 12px;
      letter-spacing: .12em;
      text-transform: uppercase;
    }

    .result-value {
      text-align: right;
      font-size: 35px;
      line-height: .9;
      letter-spacing: -.06em;
      font-weight: 700;
    }

    .result-unit {
      display: block;
      margin-top: 8px;
      color: var(--muted);
      font-family: Georgia, "Times New Roman", serif;
      font-size: 12px;
      letter-spacing: 0;
      font-weight: 400;
    }

    .total-row .result-value { font-size: 42px; }

    .per-candle {
      margin: 22px 0 0;
      padding: 15px 16px;
      border: 1px dashed rgba(33,26,20,.55);
      background: rgba(255,255,255,.23);
      font-family: Georgia, "Times New Roman", serif;
      font-size: 13px;
      line-height: 1.55;
    }

    .copy {
      width: 100%;
      margin-top: 15px;
      padding: 12px 14px;
      background: transparent;
      color: var(--ink);
      font-size: 10px;
    }

    .copy:hover { background: rgba(255,255,255,.35); }

    .formula-note {
      max-width: 950px;
      margin: 28px auto 0;
      display: grid;
      grid-template-columns: 1fr 1fr 1fr;
      gap: 1px;
      background: var(--line);
      border: 1px solid var(--line);
    }

    .note-card {
      background: var(--paper);
      padding: 22px;
    }

    .note-card strong {
      display: block;
      margin-bottom: 8px;
      font-size: 11px;
      letter-spacing: .12em;
      text-transform: uppercase;
    }

    .note-card p {
      margin: 0;
      font-family: Georgia, "Times New Roman", serif;
      color: var(--muted);
      font-size: 13px;
      line-height: 1.5;
    }

    footer {
      max-width: 1180px;
      margin: 0 auto;
      padding: 28px;
      border-top: 1px solid var(--line);
      display: flex;
      justify-content: space-between;
      gap: 20px;
      color: var(--muted);
      font-size: 10px;
      letter-spacing: .12em;
      text-transform: uppercase;
    }

    .error {
      display: none;
      margin-top: 14px;
      padding: 10px 12px;
      background: #f2d9cf;
      border: 1px solid #7a4136;
      color: #5e2a22;
      font-size: 11px;
      line-height: 1.5;
    }

    @media (max-width: 820px) {
      .nav { display: none; }
      main { padding-top: 56px; }
      .calculator { grid-template-columns: 1fr; }
      .results {
        border-left: 0;
        border-top: 1px solid var(--ink);
      }
      .formula-note { grid-template-columns: 1fr; }
      .note-card + .note-card { border-top: 1px solid var(--line); }
    }

    @media (max-width: 560px) {
      .site-header { padding-inline: 18px; }
      main { padding: 44px 14px 70px; }
      .hero { margin-bottom: 32px; }
      .hero p { font-size: 15px; }
      .inputs, .results { padding: 30px 22px; }
      .field-grid { grid-template-columns: 1fr; }
      .field.full { grid-column: auto; }
      .button-row { flex-direction: column; }
      .brand { font-size: 13px; }
      footer { padding-inline: 18px; flex-direction: column; }
      .tool-shell::before { font-size: 9px; letter-spacing: .12em; }
    }
  </style>
</head>
<body>
  <div class="announcement">Free maker tools • made for better batches</div>

  <header class="site-header">
    <a class="brand" href="https://kindlecandle.com/" aria-label="Kindle Candle home">
      <span class="brand-mark" aria-hidden="true"></span>
      <span>Kindle Candle</span>
    </a>
    <nav class="nav" aria-label="Main navigation">
      <a href="https://kindlecandle.com/">Home</a>
      <a href="#calculator">Maker Tools</a>
      <a href="https://kindlecandle.com/collections/candles">Candles</a>
    </nav>
  </header>

  <main>
    <section class="hero">
      <span class="eyebrow">Free Candle Maker Tool</span>
      <h1>Candle Fragrance<br>Load Calculator</h1>
      <p>Calculate exactly how much wax and fragrance oil you need for a single candle or a full production batch.</p>
    </section>

    <section class="tool-shell" id="calculator" aria-label="Candle fragrance load calculator">
      <div class="calculator">
        <div class="inputs">
          <h2 class="section-title">Batch Details</h2>

          <div class="field-grid">
            <div class="field">
              <label for="quantity">Quantity</label>
              <input id="quantity" type="number" min="1" step="1" value="1" inputmode="numeric" />
              <span class="help">Number of candles in your batch.</span>
            </div>

            <div class="field">
              <label for="containerSize">Container Size</label>
              <div class="input-addon">
                <input id="containerSize" type="number" min="0.01" step="0.01" value="8" inputmode="decimal" />
                <select id="unit" aria-label="Container size unit">
                  <option value="oz" selected>oz</option>
                  <option value="g">g</option>
                  <option value="lb">lb</option>
                </select>
              </div>
              <span class="help">Use the container's water capacity.</span>
            </div>

            <div class="field">
              <label for="fragranceLoad">Fragrance Load</label>
              <div class="percent-wrap">
                <input id="fragranceLoad" type="number" min="0" max="25" step="0.1" value="6" inputmode="decimal" />
                <span class="percent-sign">%</span>
              </div>
              <span class="help">Fragrance as a percentage of wax weight.</span>
            </div>

            <div class="field">
              <label for="waxGravity">Wax Gravity</label>
              <input id="waxGravity" type="number" min="0.1" max="1.5" step="0.01" value="0.86" inputmode="decimal" />
              <span class="help">Specific gravity of your wax. Check your supplier.</span>
            </div>
          </div>

          <div class="button-row">
            <button class="calculate" id="calculateBtn" type="button">Calculate Batch →</button>
            <button class="reset" id="resetBtn" type="button">Reset</button>
          </div>
          <div class="error" id="errorBox" role="alert"></div>
        </div>

        <aside class="results" aria-live="polite">
          <div>
            <h2 class="section-title">Your Batch</h2>
            <div class="result-stack">
              <div class="result-row">
                <span class="result-label">Wax</span>
                <span class="result-value"><span id="waxResult">6.49</span><span class="result-unit" id="waxUnit">ounces</span></span>
              </div>
              <div class="result-row">
                <span class="result-label">Fragrance</span>
                <span class="result-value"><span id="fragranceResult">0.39</span><span class="result-unit" id="fragranceUnit">ounces</span></span>
              </div>
              <div class="result-row total-row">
                <span class="result-label">Total</span>
                <span class="result-value"><span id="totalResult">6.88</span><span class="result-unit" id="totalUnit">ounces</span></span>
              </div>
            </div>

            <div class="per-candle" id="perCandleText">
              Per candle: <strong>6.49 oz wax</strong> + <strong>0.39 oz fragrance</strong>.
            </div>
          </div>

          <button class="copy" id="copyBtn" type="button">Copy Results</button>
        </aside>
      </div>
    </section>

    <section class="formula-note" aria-label="Calculator notes">
      <div class="note-card">
        <strong>What it calculates</strong>
        <p>Wax weight, fragrance oil weight and your full batch weight for the quantity you enter.</p>
      </div>
      <div class="note-card">
        <strong>Fragrance load</strong>
        <p>A 10% load means 10 parts fragrance oil for every 100 parts wax by weight.</p>
      </div>
      <div class="note-card">
        <strong>Maker reminder</strong>
        <p>Always follow your wax supplier's maximum fragrance load and burn-test every finished formula.</p>
      </div>
    </section>
  </main>

  <footer>
    <span>Kindle Candle • Chicago, Illinois</span>
    <span>Made for candle makers</span>
  </footer>

  <script>
    const $ = (id) => document.getElementById(id);

    const fields = {
      quantity: $('quantity'),
      containerSize: $('containerSize'),
      fragranceLoad: $('fragranceLoad'),
      waxGravity: $('waxGravity'),
      unit: $('unit')
    };

    const unitNames = {
      oz: ['ounce', 'ounces'],
      g: ['gram', 'grams'],
      lb: ['pound', 'pounds']
    };

    function decimalsFor(unit) {
      if (unit === 'g') return 1;
      if (unit === 'lb') return 3;
      return 2;
    }

    function formatNumber(value, unit) {
      const decimals = decimalsFor(unit);
      return Number(value).toLocaleString(undefined, {
        minimumFractionDigits: decimals,
        maximumFractionDigits: decimals
      });
    }

    function shortUnit(unit) {
      return unit;
    }

    function longUnit(unit, value) {
      return Math.abs(value - 1) < 0.000001 ? unitNames[unit][0] : unitNames[unit][1];
    }

    function validate(qty, size, load, gravity) {
      if (!Number.isFinite(qty) || qty < 1) return 'Enter a quantity of at least 1 candle.';
      if (!Number.isFinite(size) || size <= 0) return 'Enter a container size greater than 0.';
      if (!Number.isFinite(load) || load < 0 || load > 25) return 'Enter a fragrance load between 0% and 25%.';
      if (!Number.isFinite(gravity) || gravity <= 0 || gravity > 1.5) return 'Enter a valid wax gravity.';
      return '';
    }

    function calculate() {
      const qty = Number(fields.quantity.value);
      const size = Number(fields.containerSize.value);
      const loadPct = Number(fields.fragranceLoad.value);
      const gravity = Number(fields.waxGravity.value);
      const unit = fields.unit.value;

      const error = validate(qty, size, loadPct, gravity);
      const errorBox = $('errorBox');

      if (error) {
        errorBox.textContent = error;
        errorBox.style.display = 'block';
        return;
      }
      errorBox.style.display = 'none';

      // Container size is treated as water capacity. Gravity converts that
      // capacity to the finished candle batch weight. Fragrance load is
      // fragrance weight as a percentage of wax weight.
      const finishedWeight = size * gravity * qty;
      const load = loadPct / 100;
      const wax = finishedWeight / (1 + load);
      const fragrance = wax * load;

      const perCandleWax = wax / qty;
      const perCandleFragrance = fragrance / qty;

      $('waxResult').textContent = formatNumber(wax, unit);
      $('fragranceResult').textContent = formatNumber(fragrance, unit);
      $('totalResult').textContent = formatNumber(finishedWeight, unit);

      $('waxUnit').textContent = longUnit(unit, wax);
      $('fragranceUnit').textContent = longUnit(unit, fragrance);
      $('totalUnit').textContent = longUnit(unit, finishedWeight);

      $('perCandleText').innerHTML =
        `Per candle: <strong>${formatNumber(perCandleWax, unit)} ${shortUnit(unit)} wax</strong> + ` +
        `<strong>${formatNumber(perCandleFragrance, unit)} ${shortUnit(unit)} fragrance</strong>.`;
    }

    function reset() {
      fields.quantity.value = 1;
      fields.containerSize.value = 8;
      fields.fragranceLoad.value = 6;
      fields.waxGravity.value = 0.86;
      fields.unit.value = 'oz';
      $('errorBox').style.display = 'none';
      calculate();
    }

    async function copyResults() {
      const unit = fields.unit.value;
      const text = [
        'Kindle Candle — Fragrance Load Calculation',
        `Quantity: ${fields.quantity.value}`,
        `Container size: ${fields.containerSize.value} ${unit}`,
        `Fragrance load: ${fields.fragranceLoad.value}%`,
        `Wax gravity: ${fields.waxGravity.value}`,
        `Wax: ${$('waxResult').textContent} ${unit}`,
        `Fragrance: ${$('fragranceResult').textContent} ${unit}`,
        `Total: ${$('totalResult').textContent} ${unit}`
      ].join('\n');

      try {
        await navigator.clipboard.writeText(text);
        const btn = $('copyBtn');
        const old = btn.textContent;
        btn.textContent = 'Copied ✓';
        setTimeout(() => btn.textContent = old, 1400);
      } catch {
        window.prompt('Copy your results:', text);
      }
    }

    $('calculateBtn').addEventListener('click', calculate);
    $('resetBtn').addEventListener('click', reset);
    $('copyBtn').addEventListener('click', copyResults);

    Object.values(fields).forEach((field) => {
      field.addEventListener('keydown', (event) => {
        if (event.key === 'Enter') calculate();
      });
    });

    calculate();
  </script>
</body>
</html>
